<%= form_for(@bibref) do |f| %>
  <% if @bibref.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bibref.errors.count, "error") %> prohibited this bibref from being saved:</h2>

      <ul>
      <% @bibref.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shortname %><br>
    <%= f.text_field :shortname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
