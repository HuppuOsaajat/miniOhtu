<%= form_for(@bibref) do |f| %>

    <%= render 'form_errors' %>

  <div class="field">
    <%= f.label :shortname %><br>
    <%= f.text_field :shortname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
